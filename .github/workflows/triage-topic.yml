name: Triage proposed topic

on:
  issues:
    types: [opened]
  workflow_dispatch:
    inputs:
      issue_number:
        description: Issue number to triage (optional, auto-detects from event if omitted)
        required: false
        type: string

permissions:
  contents: read
  issues: write

concurrency:
  group: triage-topic-${{ github.event.issue.number }}
  cancel-in-progress: false

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Install topic classifier
        run: pip install ./generator

      - name: Classify proposed topic
        env:
          LLM_BASE_URL: ${{ vars.LLM_BASE_URL }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_TRIAGE_MODEL: ${{ vars.LLM_TRIAGE_MODEL }}
          TRIAGE_DAILY_LIMIT: "5"
          TRIAGE_ISSUE_NUMBER: ${{ inputs.issue_number }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: triage-topic
